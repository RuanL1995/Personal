# AI projects

This directory contains local-only AI experiments. `apiTest` is a synthetic KYC
HTTP API and `semanticTest` is a Semantic Kernel client that calls it.

These projects do not perform real identity verification and must not be used
with production personal data.
